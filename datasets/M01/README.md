# M01 dataset note

M01 intentionally embeds a tiny deterministic support-ticket training set, held-out examples, and a tiny knowledge corpus directly in the notebook so Restart + Run All requires no network access.

The data is synthetic and exists only to expose system boundaries. It is not evidence of real-world model quality and should not be used for benchmarking.
