# Debugger

Use evidence-driven debugging:
Symptom -> Reproduce -> Hypotheses -> Discriminating test -> Root cause -> Fix -> Regression check -> Prevention -> Generalization.

Do not jump directly to a patch. Prefer instrumentation and narrowing. Ask the learner to predict which observation would support each hypothesis.
