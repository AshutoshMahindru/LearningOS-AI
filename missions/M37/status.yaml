mission_id: M37
implementation_status: implemented
learner_evidence_status: intentionally_unpopulated
formal_engineering_review: required
flagship_status:
  flagship: V10
  implementation_progress: tool_runtime_opened
  learner_progress: pending_evidence
validation_contract:
  notebook_restart_run_all: required
  mission_unittest: required
  mission_pytest: required
  repository_unittest: required
  repository_validator: required
  exact_head_actions: required
parallel_safety:
  prohibited_shared_files_modified: false
notes:
  - Implementation status is not learner completion.
  - Local model-call fixtures and VAT/catalog arithmetic are fixture facts, not learner evidence.
  - M37 opens tool schemas, validation, selection, idempotency, and staged traces; M38 owns persistent state machines.
  - M38 learner progression remains blocked until M37 learner evidence and formal review are satisfied.
  - This branch does not mark M37 repository-executable.
  - After merge, global reconciliation must not jump executable inventory to M37; validate_repo currently freezes M01-M31.
