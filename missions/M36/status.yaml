mission_id: M36
implementation_status: implemented
learner_evidence_status: intentionally_unpopulated
formal_engineering_review: required
phase_end: true
flagship_status:
  flagship: V09
  implementation_progress: retrieval_infrastructure_opened
  learner_progress: pending_evidence
  phase_closed: false
validation_contract:
  notebook_restart_run_all: required
  mission_unittest: required
  mission_pytest: required
  repository_unittest: required
  repository_validator: required
  exact_head_actions: required
parallel_safety:
  prohibited_shared_files_modified: false
notes:
  - Implementation status is not learner completion.
  - Bundled metric and id properties are fixture facts, not learner evidence.
  - M36 opens vector-database and hybrid-retrieval infrastructure; M37 owns tools; M32 owns decoding; M34 owns generation.
  - V09 does not close because this package exists. Learner ADR, evidence, and formal review remain required.
