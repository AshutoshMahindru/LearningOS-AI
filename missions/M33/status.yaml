mission_id: M33
implementation_status: implemented
learner_evidence_status: intentionally_unpopulated
formal_engineering_review: required
flagship_status:
  flagship: V08
  implementation_progress: semantic_search_opened
  learner_progress: pending_evidence
validation_contract:
  notebook_restart_run_all: required
  mission_unittest: required
  mission_pytest: required
  repository_unittest: required
  repository_validator: required
  exact_head_actions: required
parallel_safety:
  prohibited_shared_files_modified: false
notes:
  - Implementation status is not learner completion.
  - Bundled ranking properties are fixture facts, not learner evidence.
  - M33 opens an exact retriever; M34 owns generation grounding; M35 owns rerank; M36 owns ANN/hybrid infra; M29 owns attention.
  - M34 learner progression remains blocked until M33 learner evidence and formal review are satisfied.
  - This branch does not mark M33 repository-executable.
  - After merge, global reconciliation must not jump executable inventory to M33 while M26 and M29-M32 gaps exist.
