# M41 Code Reading Guide: Architecture Boundaries & Contracts

## System Boundaries
Understand how `SystemBoundary` isolates data, control, trust, and system operations.

## Decision Classification
Differentiate deterministic logic (schema validation, threshold checks, rule matches) from model-driven inference (LLM generation, semantic routing).

## Degradation Strategies
Review `DegradationMode` implementations for fallback cascade handling.
